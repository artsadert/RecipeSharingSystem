version: '3.9'

services:
  database:
    image: 'postgres:latest'
    restart: always

    ports:
      - 5432:5432

    env_file:
      - .env

    volumes:
      - ./db-data:/var/lib/postgresql/data/


